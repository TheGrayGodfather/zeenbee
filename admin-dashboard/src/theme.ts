import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#FCA311",
      light: "#FFD699",
      dark: "#D18B00",
    },
    secondary: {
      main: "#14213D",
      light: "#6A7EAB",
      dark: "#0E172B",
    },
  },
});

export default theme;
